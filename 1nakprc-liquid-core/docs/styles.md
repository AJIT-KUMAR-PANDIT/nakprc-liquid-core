# styles.js

This module is responsible for managing the CSS styles required by the Nakprc Liquid Core library.

## API

### `injectNakprcStyles()`
Injects the required base styles into the document's `<head>`. It ensures that styles are only injected once, avoiding duplicates if called multiple times.
